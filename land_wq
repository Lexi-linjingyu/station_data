library(fuzzyjoin)
library(readr)
library(dplyr)
setwd("~/wq/observed_wq")
all_s <- read_excel("Jan.xls")
some_s <- read_csv("changed_row.csv")
some_s <- some_s %>% 
  select(-c(3,4,5,6,8))
colnames(all_s)[5] <- "NAME"
all <- stringdist_inner_join(some_s,all_s, by = 'NAME')
